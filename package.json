{
  "name": "executor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node dist/executor.js",
    "start:watch": "nodemon dist/executor.js",
    "build": "npm run clean && tsc",
    "build:watch": "npm run clean && tsc --watch",
    "clean": "rm -rf dist || exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.beuth-hochschule.de:scheduling-threading-javascript/executor.git"
  },
  "author": "Jannik Portz",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.7.1",
    "nodemon": "^1.19.1",
    "prettier": "^1.18.2",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  }
}
